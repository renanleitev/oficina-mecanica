import { styled } from "styled-components";
import { FlexRowContainer } from "../../styles";

export const ButtonBar = styled(FlexRowContainer)`
    margin-bottom: 10px;
    justify-content: space-around;
`;